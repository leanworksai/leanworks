# Session Manager API (FastAPI app)
fastapi==0.104.1
uvicorn==0.24.0
pydantic==2.5.0

# Workspace/session runtime (pandas, openpyxl, etc.) — single source: deploy/requirements-workspace.txt
-r requirements-workspace.txt
